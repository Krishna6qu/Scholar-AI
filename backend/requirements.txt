fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
python-multipart==0.0.9
loguru==0.7.2
litellm>=1.71.0
pypdf==4.3.1
slowapi==0.1.9
gunicorn==23.0.0
boto3==1.35.24
httpx==0.27.2

# dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.8
black==24.8.0

# --- Added back in later phases, once we build the features that need them ---
redis==5.0.8               # needed for Redis-backed rate limiting (e.g. Upstash) — see app/core/rate_limit.py
# celery==5.4.0            (Phase 7: background AI job queue — not built yet)
# qdrant-client==1.11.2    (Phase 7: vector search / real RAG)
# llama-index==0.11.14     (Phase 7: RAG - this pulled in numpy and broke your build)
# tenacity==9.0.0
